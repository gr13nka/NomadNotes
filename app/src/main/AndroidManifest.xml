<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Notebooks are stored as a plain folder tree the user can see, back up, and sync. Reaching a
        user-visible location on shared storage needs All-Files access; without it the app falls
        back to its own external files dir (see StorageRoots), so this permission is not mandatory.
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- The entry point: the notebook picker. It launches EditorActivity for a chosen notebook. -->
        <activity
            android:name=".app.NotebookListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Opened for a specific notebook (name passed as an intent extra); not the launcher. -->
        <activity
            android:name=".app.EditorActivity"
            android:exported="false" />

        <!-- Kept as the raw-drawing reference; no longer the launcher. -->
        <activity
            android:name=".app.SpikeActivity"
            android:exported="false" />
    </application>
</manifest>
